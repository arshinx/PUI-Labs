// --- Pillows --- //

// Pillow (Constructor)
function Pillow(name, description, type, shape, color, image) {
  this.name = name;
  this.description = description;
  this.type = type;
  this.shape = shape;
  this.color = color;
  this.image = image;
}
